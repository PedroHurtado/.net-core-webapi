global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'healthcheck-api'
    static_configs:
      - targets: ['host.docker.internal:5092'] # Ajusta el puerto
    metrics_path: '/metrics'
    scrape_interval: 5s